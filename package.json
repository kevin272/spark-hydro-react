{
  "name": "spark-hydro-react",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd FE && npm install --include=dev && npm run build && cd ../BE && npm install",
    "start": "cross-env NODE_ENV=production node BE/src/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.13",
    "cross-env": "^10.0.0",
    "nodemon": "^3.1.10"
  }
}
